# The credentials stored in this secret are no real credentials! They are used for testing purposes only!
# They can't be used to access any real system or service.
kind: Secret
apiVersion: v1
metadata:
  name: my-credentials
  namespace: test
type: Opaque
stringData:
    credentials: |
        {
        "username": "noRealUser",
        "password": "noRealPassword",
        }